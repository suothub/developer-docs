---
title: "SUOT — SDKs PHP para software de transporte no Brasil"
sidebarTitle: "Início"
description: "Documentação pública do Fiscal SDK e ANTT SDK. Avalie a API tipada antes de contratar. Da ordem ao resultado."
---

SUOT para Desenvolvedores — infraestrutura para quem constrói software de transporte no Brasil.

SDKs tipados para documentos fiscais e obrigações regulatórias, com DX pensada para PHP 8.4+, Laravel, IDEs e agentes de IA.

<CardGroup cols={2}>
  <Card title="Avaliar o Fiscal SDK" icon="book-open" href="/fiscal/avaliar">
    Leia a API pública, veja exemplos fluent de CT-e e entenda Result e rejeição — sem criar conta.
  </Card>
  <Card title="Contratar / Developer Portal" icon="building-columns" href="https://developers.suot.dev">
    Crie a organização, ative a licença e gere tokens Composer para instalar via packages.suot.dev.
  </Card>
</CardGroup>

<CardGroup cols={3}>
  <Card title="CT-e fluent" icon="file-invoice" href="/fiscal/cte">
    Emitir, consultar, eventos, contingência e DF-e com `$cte->…->handle()`.
  </Card>
  <Card title="Instalar com Composer" icon="download" href="/fiscal/instalacao">
    Registry privado, autenticação e `composer require` quando for integrar de verdade.
  </Card>
  <Card title="ANTT SDK" icon="truck" href="/antt/visao-geral">
    CIOT, RNTRC e obrigações regulatórias por API PHP tipada.
  </Card>
</CardGroup>

## Como funciona

Não há API REST intermediária. Você consome o SDK na sua aplicação:

```
Sua aplicação → [Laravel Bridge opcional] → Fiscal SDK → SEFAZ / serviço regulatório
```

<Note>
  A documentação é pública. Conta e licença não são pré-requisito para **avaliar** a API. Licença e token Composer entram quando você for **instalar** os pacotes no seu projeto.
</Note>

## Mental model (CT-e)

Dois entry points equivalentes — escolha um e mantenha:

```php
// A — Client fluent
$result = $cte->issue()
    ->forOperation('ot-000123')
    ->inHomologation()
    // … issuedBy, from, to, carrying, …
    ->handle();

// B — Command + Handler
$command = IssueCteCommand::create()
    ->forOperation('ot-000123')
    ->inHomologation()
    // … mesma cadeia …
    ->build();
$result = $handler->handle($command);
```

Rejeição fiscal é `Result` (`isRejected()`, `failure()`), não exception. Falha de infraestrutura (rede, certificado) é exception. O **host** persiste XML/chave e executa retry — o SDK só orienta.

## Primeiros passos

<Steps>
  <Step title="Entenda a API pública">
    Leia [API pública](/fiscal/api-publica) e a jornada [CT-e](/fiscal/cte).
  </Step>
  <Step title="Siga a trilha Avaliar">
    Veja [Avaliar o SDK](/fiscal/avaliar) — mapa da trilha `bin/dx` e o que cada fluxo ensina.
  </Step>
  <Step title="Instale quando for integrar">
    Com licença ativa, configure o registry e rode `composer require` — [Instalação](/fiscal/instalacao).
  </Step>
  <Step title="Homologue na SEFAZ">
    Ambiente de homologação, certificado e flags explícitas antes de produção — [Homologação](/guias/homologacao).
  </Step>
</Steps>

## Por que SUOT?

<CardGroup cols={2}>
  <Card title="Tipagem total" icon="code">
    PHPStan no máximo, zero métodos mágicos. Autocomplete sem configuração extra.
  </Card>
  <Card title="Pronto para IA" icon="bot">
    PHPDoc em pt-BR, enums e named arguments — agentes geram código alinhado à API.
  </Card>
  <Card title="Laravel ou agnóstico" icon="layers">
    PHP 8.4+ direto ou Laravel Bridge com DI e `config/`.
  </Card>
  <Card title="Result sem ambiguidade" icon="shield-check">
    Rejeição esperada → Result. Falha técnica → InfrastructureException.
  </Card>
</CardGroup>
