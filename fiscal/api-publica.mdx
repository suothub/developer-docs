---
title: "API pública do Fiscal SDK"
sidebarTitle: "API pública"
description: "API padrão de CT-e: $cte->…->handle(). O que é @internal. Rejeição fiscal é Result; infraestrutura é exception."
---

A superfície pública do CT-e é o client `Suot\Fiscal\Cte\Application\Cte`: monte a intenção e encerre em `handle()`.

## Client `$cte`

```php
use Suot\Fiscal\Cte\Application\Cte;
use Suot\Fiscal\Core\Exception\InfrastructureException;
use Suot\Fiscal\Core\Result\RetryMode;
use Suot\Fiscal\Core\ValueObject\Uf;

try {
    $result = $cte->serviceStatus()
        ->forOperation('status-1')
        ->inHomologation()
        ->issuerUf(new Uf('RS'))
        ->handle();
} catch (InfrastructureException $e) {
    $failure = $e->toFailure('service_status');
    // Host: alertar; filar retry conforme $failure->retryPolicy
    throw $e;
}

if ($result->isOperational()) {
    // Host: seguir para emissão ou outras operações
} else {
    $failure = $result->failure();
    $policy = $result->retryPolicy();
    match ($policy?->mode) {
        RetryMode::AfterDelay, RetryMode::Immediate => /* filar e tentar de novo */,
        RetryMode::Never, null => /* não emitir; tratar indisponibilidade */,
        default => /* seguir orientação do recoveryGuide */,
    };
}
```

Portas no mesmo client: `issue()`, `validate()`, `cancel()`, `correct()`, `serviceStatus()`, `byAccessKey()`, `authorizationProtocol()`, `downloadProc()`, `distributeDfe()`, `registerEpec()`, `issueInContingency()`, eventos de entrega, etc.

Namespace: `Suot\Fiscal\Cte\Application\Cte`.

## Client `$cteOs` (modelo 67)

CT-e OS usa o mesmo padrão com `Suot\Fiscal\CteOs\Application\CteOs`:

```php
use Suot\Fiscal\CteOs\Application\CteOs;

// $cteOs injetado via DI ou Laravel Bridge (alias fiscal.cte-os)
$result = $cteOs->issue()
    ->forOperation('charter-1')
    ->inHomologation()
    // … cadeia tipada …
    ->handle();
```

Portas: `serviceStatus()`, `issue()`, `byAccessKey()`, `cancel()`. Jornada completa: [CT-e OS](/fiscal/cte-os).

## Client `$bpe` (modelo 63)

BP-e usa o mesmo padrão com `Suot\Fiscal\Bpe\Application\Bpe`:

```php
use Suot\Fiscal\Bpe\Application\Bpe;

// $bpe injetado via DI (Laravel Bridge em evolução)
$result = $bpe->issue()
    ->forOperation('ticket-1')
    ->inHomologation()
    // … cadeia tipada …
    ->handle();
```

Portas: `serviceStatus()`, `issue()`, `byAccessKey()`, `cancel()`. Jornada completa: [BP-e](/fiscal/bpe). BP-e TM e TA **não** estão disponíveis nesta onda.

## Client `$mdfe` (modelo 58)

MDF-e usa o mesmo padrão com `Suot\Fiscal\Mdfe\Application\Mdfe`:

```php
use Suot\Fiscal\Mdfe\Application\Mdfe;

// $mdfe injetado via DI ou Laravel Bridge (alias fiscal.mdfe)
$result = $mdfe->issue()
    ->forOperation('manifest-1')
    ->inHomologation()
    // … cadeia tipada …
    ->handle();
```

Portas: `serviceStatus()`, `issue()`, `byAccessKey()`, `nonClosed()`, `cancel()`, `close()`, `includeDriver()`, `includeDfe()`, `issueInContingency()`, `downloadProc()`, `distributeDfe()`. Jornada completa: [MDF-e](/fiscal/mdfe).

No Laravel, Handlers explícitos são opcionais — ver [Laravel Bridge](/fiscal/laravel-bridge).

## O que é @internal

Não use na aplicação hospedeira:

| Evitar | Motivo |
|---|---|
| `Operation/*Service`, `*Input`, `*::execute()` | Pipeline fiscal interno |
| `*InputMapper`, `IssueCteCommandBuilder`, `IssueCteCommandParts` | Wiring atrás do Handler |
| `CTeBuilder`, `EmitirCTe*`, `AutorizarNFe*`, `AutorizarCte*` | Não existem na API pública |
| `CteClient` como porta principal | A porta pública é `Cte` |

<Warning>
Rejeição fiscal válida volta como `Result` (`isRejected()`, `failure()`), não como exception. Exceptions são para infraestrutura, argumentos inválidos e estados impossíveis.
</Warning>

## Próximos passos

- [Avaliar sem conta](/fiscal/avaliar) — trilha de aprendizado da API
- [CT-e](/fiscal/cte) — jornada do documento
- [MDF-e](/fiscal/mdfe) — manifesto e modelo 58
- [CT-e OS](/fiscal/cte-os) — fretamento e modelo 67
- [BP-e](/fiscal/bpe) — passagem rodoviária e modelo 63
- [Instalação](/fiscal/instalacao) — Composer + registry
